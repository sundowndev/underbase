{
  "name": "af-models",
  "version": "1.0.1",
  "description": "Airfordable Data Models for mongodb",
  "main": "dist/index.js",
  "scripts": {
    "start": "nodemon src/index.js --exec babel-register",
    "compile": "babel src -d dist --presets es2015,stage-0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Emmanuel Buah <emmanuel@airfordable.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.16.0",
    "babel-register-cli": "^2.0.1",
    "nodemon": "^1.10.2"
  },
  "dependencies": {
    "mongoose": "^4.6.1"
  }
}
